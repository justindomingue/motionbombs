$('#user_activity').append('<%= j render(@user_activity) %>');
<% if @user_activity.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@user_activity) %>');
<% else %>
  $('.pagination').remove();
<% end %>