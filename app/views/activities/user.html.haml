.page-header
  %h1
    Activity feed for 
    = @user.username

%table.table.table-striped
  %tbody#user_activity
    = render :partial => 'activity', :collection => @activity

= will_paginate @activity
